name: Deploy to Azure App Service (Production)

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Allows manual deployment from GitHub UI

env:
  AZURE_WEBAPP_NAME: healthmesh
  NODE_VERSION: '22.x'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: � Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: �🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: 📦 Install dependencies
      run: npm ci --legacy-peer-deps

    - name: 🔨 Build application
      env:
        VITE_AZURE_AD_CLIENT_ID: 6116990b-c0d4-42fd-bb31-006498740273
        VITE_AZURE_AD_TENANT_ID: e290fb02-d184-4a8c-ae49-c83b04485909
        VITE_AZURE_AD_REDIRECT_URI: https://healthmesh.azurewebsites.net/login
        NODE_ENV: production
      run: npm run build

    - name: 📁 Install production dependencies
      run: npm ci --production --ignore-scripts --legacy-peer-deps

    - name: 📦 Create deployment ZIP
      run: |
        echo "📦 Creating deployment ZIP..."
        
        # Create a simple ZIP with everything needed
        zip -r deploy.zip dist node_modules shared package.json -q
        
        echo "✅ ZIP created: $(du -h deploy.zip | cut -f1)"

    - name: 🔐 Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: 🚀 Upload deployment package
      run: |
        # Get publishing credentials
        echo "Getting deployment credentials..."
        CREDS=$(az webapp deployment list-publishing-credentials \
          --resource-group HealthMesh \
          --name healthmesh \
          --query "{username:publishingUserName,password:publishingPassword}" \
          --output json)
        
        USERNAME=$(echo $CREDS | jq -r .username)
        PASSWORD=$(echo $CREDS | jq -r .password)
        
        # Upload using Kudu REST API (most reliable method)
        echo "Uploading deployment package..."
        curl -X POST \
          -u "$USERNAME:$PASSWORD" \
          --data-binary @deploy.zip \
          https://healthmesh.scm.azurewebsites.net/api/zipdeploy \
          -H "Content-Type: application/zip" \
          --fail \
          --show-error \
          --max-time 600

    - name: ⏳ Wait for deployment
      run: |
        echo "Waiting for deployment to complete..."
        sleep 20

    - name: 🔄 Restart app
      run: |
        echo "Restarting application..."
        az webapp restart \
          --resource-group HealthMesh \
          --name healthmesh

    - name: ✅ Deployment complete
      run: |
        echo "🎉 Deployment successful!"
        echo "🌐 Application URL: https://healthmesh.azurewebsites.net"
        echo "⏰ Deployed at: $(date)"
        
        echo "⏳ Waiting for app to be ready..."
        sleep 15
        
        echo "🔍 Testing application..."
        curl -I https://healthmesh.azurewebsites.net || echo "App is starting (this is normal)"


