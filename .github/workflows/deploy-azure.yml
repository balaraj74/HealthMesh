name: Deploy to Azure App Service

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Allows manual deployment from GitHub UI

env:
  AZURE_WEBAPP_NAME: healthmesh-dev-app
  NODE_VERSION: '20.x'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: 📦 Install dependencies
      run: npm ci

    - name: 🔨 Build application
      env:
        VITE_AZURE_AD_REDIRECT_URI: https://healthmesh-dev-app.azurewebsites.net/login
        NODE_ENV: production
      run: npm run build

    - name: 📁 Install production dependencies
      run: npm ci --production --ignore-scripts

    - name: 📦 Prepare deployment package
      run: |
        # Create a clean deployment directory
        mkdir -p deployment
        
        # Copy built application
        cp -r dist deployment/
        
        # Copy production node_modules
        cp -r node_modules deployment/
        
        # Copy package files
        cp package.json deployment/
        cp package-lock.json deployment/
        
        # Create .deployment file to tell Azure not to build
        echo "[config]" > deployment/.deployment
        echo "SCM_DO_BUILD_DURING_DEPLOYMENT=false" >> deployment/.deployment
        
        # Zip the deployment package
        cd deployment
        zip -r ../deploy.zip . -q
        cd ..
        echo "✅ Deployment package created: $(du -h deploy.zip | cut -f1)"

    - name: 🚀 Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: deploy.zip

    - name: ✅ Deployment complete
      run: |
        echo "🎉 Deployment successful!"
        echo "🌐 Application URL: https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net"
        echo "⏰ Deployed at: $(date)"
